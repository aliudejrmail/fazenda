{
  "name": "fazenda",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "api:dev": "npm --prefix apps/api run start:dev",
    "api:seed": "npm --prefix apps/api run prisma:seed",
    "web:dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web install && npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "build:api": "npm --prefix apps/api install --include=dev && npm --prefix apps/api exec prisma generate && npm --prefix apps/api run build",
    "start:api": "npm --prefix apps/api run start:render",
    "build:web": "npm --prefix apps/web install && npm --prefix apps/web run build",
    "start:web": "npm --prefix apps/web run start"
  }
}
